# install.packages("gitcreds")
library(gitcreds)
gitcreds_get()
gitcreds_set()    
2
# I could set up a new token , so I don't need the PW anymore.
# ignore xlsx is working as a safety measure ! 
# so it doesnt make any changes to the original